idea:
    build: .
    labels:
        org.kurron.description: "logFaces client"
        org.kurron.version: "4.2.1"
    container_name: "logfaces"
    net: "host"
    volumes:
        - /tmp/.X11-unix:/tmp/.X11-unix
        - /home/vagrant:/home/developer
    environment:
        DISPLAY: unix:0.0
    user: 1000:1000
